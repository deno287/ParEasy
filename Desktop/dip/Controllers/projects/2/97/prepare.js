var in_matrix = [];
var out_matrix = [];

in_matrix = parallel.binFill(in_matrix,10);
out_matrix = parallel.binFill(out_matrix, 10);

inB = parallel.copy(in_matrix);
outB = parallel.copy(out_matrix);



